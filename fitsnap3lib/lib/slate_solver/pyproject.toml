
[build-system]
requires = [
    "setuptools",
    "wheel",
    "Cython>=3.0",
    "numpy"
]
build-backend = "setuptools.build_meta"

[project]
name = "slate_solver"
version = "0.1.0"
description = "Cython wrapper for SLATE solver in FitSNAP"
dependencies = ["numpy", "mpi4py"]
